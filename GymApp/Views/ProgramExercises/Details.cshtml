@model ProgramExercise

@{
    ViewData["Title"] = "Detalji relacije";
}

<section class="detail-shell">
    <header class="detail-header">
        <h1>@Model.Program?.Name</h1>
        <div class="d-flex gap-2">
            <a class="btn-theme-secondary" asp-action="Index">Natrag na listu</a>
            <a class="btn-theme-primary" asp-action="Edit" asp-route-id="@Model.Id">Uredi</a>
            <a class="btn-theme-secondary" asp-action="Delete" asp-route-id="@Model.Id">Obrisi</a>
        </div>
    </header>

    <div class="detail-grid single-col">
        <article class="surface-panel">
            <h2>Program + vjezba</h2>
            <p>Program: @Model.Program?.Name</p>
            <p>Vjezba: @Model.Exercise?.Name</p>
            <p>Dan: @Model.DayOfWeek</p>
            <p>Cilj: @Model.TargetSets setova x @Model.TargetReps reps</p>
        </article>
    </div>
</section>
