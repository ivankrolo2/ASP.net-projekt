@model BodyMeasurement

@{
    ViewData["Title"] = "Detalji mjerenja";
}

<section class="detail-shell">
    <header class="detail-header">
        <h1>@Model.User?.FirstName @Model.User?.LastName</h1>
        <div class="d-flex gap-2">
            <a class="btn-theme-secondary" asp-action="Index">Natrag na listu</a>
            <a class="btn-theme-primary" asp-action="Edit" asp-route-id="@Model.Id">Uredi</a>
            <a class="btn-theme-secondary" asp-action="Delete" asp-route-id="@Model.Id">Obrisi</a>
        </div>
    </header>

    <div class="detail-grid single-col">
        <article class="surface-panel">
            <h2>Mjerenje</h2>
            <p>Datum: @Model.RecordedAt.ToString("dd.MM.yyyy")</p>
            <p>Tjelesna masa: @Model.BodyWeightKg.ToString("F1") kg</p>
            <p>Body fat: @Model.BodyFatPercentage.ToString("F1")%</p>
            <p>Korisnik ID: @Model.User?.Id</p>
        </article>
    </div>
</section>
